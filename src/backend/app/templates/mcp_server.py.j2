# Generated by MCPForge — https://mcpforge.dev
# API: {{ metadata.title }} ({{ metadata.version }})
# Base URL: {{ metadata.base_url }}
#
# Install dependencies:
#   pip install fastmcp httpx

import httpx
from fastmcp import FastMCP

mcp = FastMCP("{{ metadata.title }}")

BASE_URL = "{{ metadata.base_url }}"

{% if auth %}
# Authentication
# TODO: template auth setup block
{% endif %}

{% for card in tool_cards %}
{% if card.enabled %}
@mcp.tool()
def {{ card.tool_name }}(
    {%- for param in card.parameters %}
    {{ param.name }}: {{ param.type }}{% if not param.required %} = None{% endif %},
    {%- endfor %}
) -> dict:
    """{{ card.description }}"""
    # TODO: template HTTP call block
    raise NotImplementedError

{% endif %}
{% endfor %}

if __name__ == "__main__":
    mcp.run()
